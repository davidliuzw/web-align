build:
  docker:
    web: ./backend/Dockerfile
    worker: ./frontend/Dockerfile

release:
  command:
    - docker run -d -p 8000:8000 registry.heroku.com/webalign/backend 
    - docker run -d -p 3000:3000 registry.heroku.com/webalign/frontend

